"""Basic legal constants used by the demo application.

This file only contains a couple of example values so tests can import
``LAW_SECTIONS`` without failing.  In a real project this module would be
populated with the full set of articles and associated metadata.
"""

# Minimal example mapping of law article numbers to titles
LAW_SECTIONS = {
    "Art. 1": "Preliminary provisions",
    "Art. 2": "Definitions",
}

